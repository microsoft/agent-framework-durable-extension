<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFrameworks>$(TargetFrameworksCore)</TargetFrameworks>
    <ImplicitUsings>enable</ImplicitUsings>
    <IsReleaseCandidate>true</IsReleaseCandidate>
    <!-- CA2007: This rule should generally be suppressed in Durable Task libraries -->
    <NoWarn>$(NoWarn);CA2007</NoWarn>
  </PropertyGroup>

  <Import Project="$(RepoRoot)/dotnet/nuget/nuget-package.props" />

  <!-- NuGet package metadata -->
  <PropertyGroup>
    <Title>Durable Task extensions for Microsoft Agent Framework</Title>
    <Description>Provides distributed durable execution capabilities for agents built with Microsoft Agent Framework.</Description>
    <PackageReadmeFile>README.md</PackageReadmeFile>
  </PropertyGroup>

  <PropertyGroup>
    <InjectSharedStructuredOutput>true</InjectSharedStructuredOutput>
  </PropertyGroup>
  
  <!-- Durable Task dependencies -->
  <ItemGroup>
    <PackageReference Include="Microsoft.DurableTask.Client" />
    <PackageReference Include="Microsoft.DurableTask.Worker" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.Agents.AI.Workflows" />
    <PackageReference Include="Microsoft.Agents.AI" />
  </ItemGroup>

  <ItemGroup>
    <InternalsVisibleTo Include="Microsoft.Agents.AI.DurableTask.IntegrationTests, PublicKey=$(MicrosoftAgentFrameworkPublicKey)" />
    <InternalsVisibleTo Include="Microsoft.Agents.AI.DurableTask.UnitTests, PublicKey=$(MicrosoftAgentFrameworkPublicKey)" />
    <InternalsVisibleTo Include="Microsoft.Agents.AI.Hosting.AzureFunctions, PublicKey=$(MicrosoftAgentFrameworkPublicKey)" />
  </ItemGroup>

  <ItemGroup>
    <None Include="README.md" Pack="true" PackagePath="/" />
  </ItemGroup>
</Project>
